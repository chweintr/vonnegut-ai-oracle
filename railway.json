{
  "build": {
    "commands": [
      "pip install -r requirements.txt"
    ]
  },
  "deploy": {
    "startCommand": "streamlit run vonnebot_clean.py --server.port $PORT --server.address 0.0.0.0"
  }
}
